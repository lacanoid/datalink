[Unit]
Description=PostgreSQL SQL/MED Datalink File Filter for FUSE
After=postgresql.service

[Service]
User=root
WorkingDirectory=/var/lib/postgresql
ExecStart=pg_dlfuse -f -o allow_other /dlfs
Restart=always

[Install]
WantedBy=multi-user.target
